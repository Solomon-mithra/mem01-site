/** Footer — huge grainy fading mem01 only (no border / chrome) */
export function Footer() {
  return (
    <footer className="relative overflow-hidden">
      <div className="footer-mega-wrap select-none">
        <p className="footer-mega-word" aria-hidden>
          mem01
        </p>
      </div>
    </footer>
  );
}
